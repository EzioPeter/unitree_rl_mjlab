# Unitree／Go2 项目强制执行规则

本目录及其所有子项目的最高原则是实验正确、有效、可解释和可复现。

任何训练、评测、数据采集、转换、导出或部署操作前，必须完整阅读并遵守：

```text
/home/xjy/Go2/EXPERIMENT_INTEGRITY_FIRST.md
```

关键配置缺少当前直接证据、参考实验不明确或配置 diff 含非预期变化时，必须停止，
禁止猜测并禁止启动。正确性永远优先于速度、GPU 利用率、并行数量和进度。

实验设置的正确性和有效性至关重要。每次完成任何代码、配置、路线文档、数据流程、
训练、评测或部署相关改动后，必须仔细检查改动内容及其对实验设置的影响，确认不存在
维度、监督语义、数据来源、buffer 比例、初始化、reward、训练预算、评测协议或
artifact 绑定错误。检查完成后，必须向用户明确输出检查反馈，包括已检查的内容、
检查结果、仍未冻结的配置和任何证据缺口；不得只报告“已修改”而省略验证反馈。
