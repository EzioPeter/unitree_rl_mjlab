"""RWM-specific task registrations.

This package intentionally lives beside, rather than inside, the existing
velocity task package so the stock Go2/G1 tasks remain untouched.
"""

from .config import *  # noqa: F403
